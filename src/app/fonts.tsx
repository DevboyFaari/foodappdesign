import { Inter, Alice } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const alice = Alice({
  subsets: ["latin"],
  display: "swap",
  weight: "400",
});
